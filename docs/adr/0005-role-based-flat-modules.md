# 역할 중심 평면 모듈

FSD 계층은 하나의 실행 흐름을 여러 계층과 공개 진입점으로 나눠 화면에서 외부 경계까지 추적하기 어렵게 했다. 코드는 화면 소유권과 제품 역할에 따라 배치하고 전달만 하는 배럴, 호환 wrapper와 계층별 공개 진입점을 만들지 않는다.

FSD를 유지하거나 배럴만 줄이는 안은 계층별 책임과 전달 단계를 남기므로 제외했다. 현재 배치 규칙은 [`SYSTEM_DESIGN.md`](../SYSTEM_DESIGN.md)를 따른다.
