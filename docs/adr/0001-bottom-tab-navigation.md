# 하단 탭과 집중 화면

홈, 목록, 캘린더와 설정은 하단 탭의 주요 목적지로 둔다. 일정 추가는 지속되는 영역이 아닌 공통 동작이므로 별도 탭으로 만들지 않고, 상세·생성·수정은 작업 흐름이 흐려지지 않도록 탭 밖의 집중 화면으로 연다. 선택한 탭을 다시 눌러도 root reset이나 scroll-to-top을 실행하지 않는다.

iOS는 시스템 네이티브 탭, Android는 커스텀 탭을 유지한다. 모든 화면에 탭을 노출하거나 생성을 탭으로 두는 안은 목적지 전환과 일정 관리 동작을 섞으므로 제외했다.
