# Ttokttak

반복되는 생활 항목을 일정으로 관리하고 실행 기록을 남기는 개인 리마인더 앱이다.

## Language

**일정**:
사용자가 관리하는 반복 생활 항목의 사용자-facing 이름.
_Avoid_: 습관, 할 일

**occurrence**:
반복 일정에서 계산된 개별 예정 시점.
_Avoid_: 일정 인스턴스, 회차

**occurrence projection**:
화면이 서로 다른 UI로 표시하기 전에 같은 기준으로 고른 occurrence 묶음.
_Avoid_: 화면 데이터, 뷰 모델, 섹션 모델

**완료**:
사용자가 occurrence를 실제로 실행했음을 남기는 처리.
_Avoid_: 체크, 달성

**건너뛰기**:
사용자가 occurrence를 이번에는 실행하지 않기로 남기는 처리.
_Avoid_: 취소, 삭제

**오늘**:
오늘 날짜에 처리할 수 있는 scheduled occurrence의 사용자-facing 섹션 이름.
_Avoid_: Today

**다가오는 일정**:
오늘 이후 예정된 scheduled occurrence의 사용자-facing 섹션 이름.
_Avoid_: Upcoming

**지난 일정**:
예정 시각이 지났지만 아직 완료 또는 건너뛰기 처리되지 않은 occurrence의 사용자-facing 이름.
_Avoid_: 놓친 일정, 일정 놓침, 밀린 일정

**홈 피드**:
사용자가 처리 가능한 occurrence를 확인하고 완료 또는 건너뛰기 처리하는 주 화면.
_Avoid_: 대시보드, 메인

**홈 피드 occurrence 처리 흐름**:
홈 피드에서 사용자의 완료 또는 건너뛰기 입력을 occurrence 처리 기록 생성, 기기 로컬 알림 재동기화, 홈 피드 재조회로 이어주는 내부 흐름.
_Avoid_: 홈 액션 훅, 완료 서비스, 피드 mutation

**일정 변경 mutation 흐름**:
일정 생성, 수정, 삭제로 보이는 보관 저장을 서버 저장, 기기 로컬 알림 재동기화, 일정 데이터 재조회로 이어주는 내부 흐름.
_Avoid_: form submit, detail delete, 저장 hook

**일정 수정 정책**:
일정 수정 입력을 기존 일정과 병합해 규칙 변경 여부, 수정 적용 시각, 다음 future occurrence 시작일을 정하는 내부 규칙.
_Avoid_: repository update policy, version helper, seed 계산

**일정 persistence**:
서버 저장소의 일정 row, schedule version, 일정 수정에 필요한 completion log를 읽고 쓰는 저장 Adapter Seam.
_Avoid_: Supabase client, repository chain, RPC wrapper

**상세 화면**:
일정의 현재 상태, 설정 요약, 최근 히스토리, 관리 동작을 확인하는 화면.
_Avoid_: 처리 화면

**상세 진입 맥락**:
사용자가 어떤 occurrence에서 상세 화면으로 들어왔는지 알려주는 기준.
_Avoid_: 처리 대상

**탭 루트**:
하단 탭에서 바로 진입하는 홈, 목록, 캘린더, 설정의 기준 화면.
_Avoid_: 이전 화면, 스택 화면

**집중 화면**:
알림함, 상세, 생성, 수정처럼 하단 탭을 숨기고 한 작업에 집중하는 화면.
_Avoid_: 탭 화면

**기기 로컬 알림**:
사용자 기기에서 예약하고 사용자 기기 OS가 예정 시각에 표시하는 알림.
_Avoid_: 서버 알림, 원격 푸시

**기기 로컬 알림 lifecycle**:
앱 시작, 세션 복원, foreground 복귀, 알림 tap, mutation 이후 현재 기기의 로컬 알림 예약을 언제 다시 맞출지 정하는 내부 흐름.
_Avoid_: 알림 bootstrap, 알림 hook, 알림 effect

**원격 푸시**:
서버가 APNs 또는 FCM 같은 푸시 provider를 통해 사용자 기기로 보내는 보조 알림 경로.
_Avoid_: 알림 기본 경로, 로컬 알림

**서버 저장 내용 복구 경계**:
서버 DB와 운영자가 접근하는 저장 표면만으로 일정 제목과 설명 평문을 복구할 수 없어야 한다는 privacy boundary.
_Avoid_: 엄격한 E2EE, 단순 암호화, 난독화

**서버 측 내용 복구**:
인증된 사용자가 새 기기에서 일정 제목과 설명을 다시 볼 수 있도록 서버 실행 경계가 암호화 key 복구를 돕는 흐름.
_Avoid_: E2EE 복구, DB 복호화, 앱 정적 key 복구

**복구 감사 이벤트**:
**서버 측 내용 복구** 호출 결과를 평문 내용이나 key 없이 추적하기 위한 운영 기록.
_Avoid_: 사용자 알림, 복구 로그, 디버그 로그, 내용 로그

**일정 색상**:
사용자가 여러 화면에서 일정을 구분하기 위해 7색 파스텔톤 무지개 팔레트에서 일정에 부여하는 표시 색상.
_Avoid_: 상태 색상, occurrence 색상, 자유 색상, 카테고리 색상

**기본 일정 색상**:
사용자가 색상을 직접 고르기 전 일정에 자동으로 부여되는 빨강 일정 색상.
_Avoid_: 무색, 임시 색상

**브랜드/UI 팔레트**:
차분한 생활 관리와 선명한 실행감을 표현하기 위해 앱의 배경, 텍스트, 강조, 액션, 상태 표현에 사용하는 제품 고유의 시각 색상 체계.
_Avoid_: 일정 색상, 레퍼런스 색상, 복사한 색상

## Relationships

- 하나의 **일정**은 여러 **occurrence**를 만든다.
- 하나의 **occurrence projection**은 여러 화면에서 재사용할 occurrence 선택 기준을 담는다.
- **occurrence projection**은 화면별 UI 모양이나 문구를 정하지 않는다.
- 하나의 **일정**은 하나의 **일정 색상**을 가진다.
- 새 **일정**은 **기본 일정 색상**을 가진 상태로 생성된다.
- **일정 색상** 팔레트는 빨강, 주황, 노랑, 초록, 파랑, 남색, 보라로 구성한다.
- **일정 색상**은 occurrence 상태가 바뀌어도 유지된다.
- **일정 색상**은 현재 정해진 팔레트 안에서만 선택한다.
- **일정 색상**은 사용자에게 한국어 색상 이름으로 표시한다.
- **일정 색상**은 카테고리나 용도 분류를 뜻하지 않는다.
- 캘린더 월간 날짜 셀의 색상 선은 occurrence 상태가 아니라 **일정 색상**을 표시한다.
- 캘린더는 **일정 색상**을 별도 범례로 설명하지 않는다.
- **일정 색상**은 캘린더, 일정 목록, 상세 화면에서 같은 일정을 식별하는 보조 표시로 사용한다.
- **홈 피드**는 occurrence 처리를 우선하므로 **일정 색상** 표시 범위에서 제외한다.
- 사용자는 일정 생성 또는 수정 중 제목과 설명을 입력한 뒤 **일정 색상**을 고를 수 있다.
- **기본 일정 색상**은 모든 새 일정에 같은 값으로 적용한다.
- **브랜드/UI 팔레트**는 **일정 색상** 팔레트와 별도로 정의한다.
- **브랜드/UI 팔레트**를 바꾸더라도 **일정 색상** key와 한국어 색상 이름은 유지한다.
- **브랜드/UI 팔레트**는 **일정 색상**의 파스텔 무지개 톤과 혼동되지 않아야 한다.
- **브랜드/UI 팔레트**는 색상 이름보다 배경, 표면, 텍스트, 주요 액션, 보조 액션, 상태처럼 UI 역할을 기준으로 부른다.
- 하나의 **occurrence**는 최대 한 번 **완료** 또는 **건너뛰기** 처리된다.
- **오늘**, **다가오는 일정**, **지난 일정**은 홈 피드의 기본 섹션이다.
- **지난 일정**은 내부 상태로 `overdue`를 사용한다.
- **완료**와 **건너뛰기**는 **홈 피드**에서만 수행한다.
- **홈 피드 occurrence 처리 흐름**은 **홈 피드**의 **완료**와 **건너뛰기**를 기록 생성, **기기 로컬 알림** 재동기화, 홈 피드 재조회와 연결한다.
- **홈 피드 occurrence 처리 흐름**은 이미 **완료** 또는 **건너뛰기** 처리된 occurrence에 새 기록을 만들지 않는다.
- **홈 피드 occurrence 처리 흐름**은 **지난 일정** occurrence를 처리할 때 그 이전 unresolved **지난 일정**도 함께 처리한다.
- **일정 변경 mutation 흐름**은 **일정** 생성, 수정, 삭제로 보이는 보관 저장 뒤 영향을 받은 **일정** 범위의 **기기 로컬 알림**을 재동기화한다.
- **일정 변경 mutation 흐름**은 **기기 로컬 알림** 재동기화 뒤 **일정** 데이터를 다시 읽도록 만든다.
- **일정 수정 정책**은 규칙 영향 필드가 바뀐 경우에만 새 schedule version 기준 값을 만든다.
- **일정 수정 정책**은 기존 **일정**의 시작일을 수정 입력으로 바꾸지 않는다.
- **일정 persistence**는 Supabase table, RPC, row column 이름을 repository의 저장 의미 Interface 뒤에 숨긴다.
- **일정 persistence**는 repository가 암호화, 복호화, 수정 정책 같은 도메인 저장 흐름을 Supabase 호출 모양 없이 테스트할 수 있게 한다.
- **상세 화면**은 occurrence 처리를 수행하지 않는다.
- **상세 진입 맥락**은 **상세 화면**의 대표 상태를 정할 수 있지만, **완료** 또는 **건너뛰기** 대상을 만들지는 않는다.
- **탭 루트**는 서로의 이전 화면이 아니라 동등한 하단 탭 목적지다.
- **집중 화면**은 필요할 때 이전 화면으로 돌아갈 수 있지만 하단 탭 목적지는 아니다.
- 하단 탭은 **탭 루트**에서만 노출한다.
- 하단 탭을 전환해도 각 영역의 현재 화면 상태는 보존한다.
- 같은 하단 탭을 다시 누르면 해당 영역의 **탭 루트**로 돌아간다.
- **기기 로컬 알림**은 일정 알림의 기본 경로다.
- **기기 로컬 알림**은 기본 30일 rolling window와 일정별 다음 occurrence 1개를 예약한다.
- **기기 로컬 알림 lifecycle**은 세션 복원, 앱 foreground 복귀, 알림 tap 뒤 현재 기기의 **기기 로컬 알림**을 전체 재동기화한다.
- **기기 로컬 알림 lifecycle**은 일정 생성, 수정, 보관, **완료**, **건너뛰기** 뒤 영향을 받은 **일정** 범위의 **기기 로컬 알림**을 재동기화한다.
- **기기 로컬 알림 lifecycle**은 알림 tap 시 user session이 아직 준비되지 않았으면 재동기화를 보류하고 user session이 준비된 뒤 한 번 실행한다.
- **기기 로컬 알림 lifecycle**은 로그아웃 또는 세션 없음 상태로 전환될 때 현재 기기에 예약된 Ttokttak **기기 로컬 알림**을 전부 취소한다.
- **기기 로컬 알림 lifecycle**의 로그아웃 정리 실패는 기록하되 로그아웃 자체를 막지 않는다.
- **원격 푸시**는 기본 알림 경로가 아니며, 나중에 필요할 때 일반 문구 fallback으로만 검토한다.
- 일정 제목과 설명은 **원격 푸시** payload에 싣지 않는다.
- 일정 제목과 설명은 **서버 저장 내용 복구 경계** 안에 있다.
- **서버 저장 내용 복구 경계**는 별도 복구 비밀번호 없이 새 기기에서 복구할 수 있다는 사용성 요구와 함께 유지한다.
- **서버 측 내용 복구**는 허용하지만, 서버 DB table만으로 일정 제목과 설명을 복구할 수 있게 만들지는 않는다.
- **서버 측 내용 복구**는 평문 내용과 content key를 남기지 않는 **복구 감사 이벤트**를 남긴다.
- 알림함과 알림 기록은 MVP 화면에서 제거한다.
- 놓친 일정은 알림함이 아니라 홈 피드와 overdue 상태로 다시 드러낸다.

## Example Dialogue

> **Dev:** "알림에서 일정 상세로 들어온 사용자가 바로 완료할 수 있어야 하나요?"
> **Domain expert:** "아니요. 상세 화면은 상태와 기록을 확인하는 곳이고, 완료나 건너뛰기는 홈 피드에서만 처리합니다."

> **Dev:** "서버에서 일정 이름을 넣어 원격 푸시를 보내도 되나요?"
> **Domain expert:** "아니요. 알림은 기기 로컬 알림을 기본으로 하고, 원격 푸시는 실제 일정 이름이나 설명을 담지 않습니다."

## Flagged Ambiguities

- "상세에서 액션"은 **완료**와 **건너뛰기** 같은 occurrence 처리를 뜻한다. 일정 수정과 삭제 같은 관리 동작은 상세 화면에 남긴다.
- "탭 화면"은 **탭 루트**와 **집중 화면**을 섞어 말하기 쉬우므로, 하단 탭 목적지는 **탭 루트**로 부른다.
- "알림함"은 서버에 동기화된 원격 푸시 inbox로 오해하기 쉬우므로 MVP 화면에서 제거한다.
