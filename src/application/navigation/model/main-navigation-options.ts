export const mainTabsRootScreenOptions = {
  gestureEnabled: false,
} as const;

export const mainTabStackScreenOptions = {
  animation: "default",
  gestureEnabled: false,
  headerShown: false,
} as const;
